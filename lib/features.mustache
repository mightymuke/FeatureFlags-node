'use strict';
/**
* Don't modify this file, it's generated by features.mustashe
*
*/
var _ = require('lodash');
function loadFeaturesFile(env) {
  var file = __dirname + '/../config/features.' + env + '.json';
  var content = null;
  try {
    content = require(file);
  } catch (e) {
    console.error(e);
    process.exit(1);
  }
  return content;
}
var environment = process.env.ENVIRONMENT || 'dev';
var features = loadFeaturesFile(environment);
function getFlag(flag) {
  return _.find(features.Features, { name: flag }).enabled;
}
module.exports = {
  {{#Features}}
    {{name}}: function() { return getFlag('{{name}}'); },
  {{/Features}}
}
